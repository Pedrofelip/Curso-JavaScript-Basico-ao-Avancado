//Faça uma estrutura de of else que verifique se um carros está acima da velocidade permitida(80)

let velocidade = 200;

if(velocidade >= 80){
    console.log('você está acima da velocidade permitida')
}else{
    console.log('você está dentro da velocidade da via')
}